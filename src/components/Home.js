
import Map from "./Map"
// import { useContext } from "react"
// import { contextUser } from "../App"

const Home = () => {

    //const {user} = useContext(contextUser)


    return (
        <div className="home">
            <Map/>
        </div>
    )
}

export default Home